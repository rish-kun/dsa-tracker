# Supabase Postgres connection string (project-specific name so a generic
# DATABASE_URL exported in your shell profile can never shadow it).
# Direct URI: postgresql://postgres:<password>@db.<ref>.supabase.co:5432/postgres
# or the "Transaction pooler" URI from Supabase dashboard → Connect.
DSA_TRACKER_DATABASE_URL=
